import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Context Query - React State Management Library",
  description: "Simple and powerful React state management library",
  keywords: "React, State Management, Context Query, TypeScript",
  authors: [{ name: "load28" }],
  openGraph: {
    title: "Context Query",
    description: "Simple and powerful React state management library",
    type: "website",
    url: "https://context-query.dev",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
